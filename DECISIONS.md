# Decisions

*Durable decisions with rationale. Only product, soul, design, architecture, data, security, or quality decisions. Not trivial choices.*

| Date | Status | Decision | Context | Alternatives | Reason | Consequences |
|------|--------|----------|---------|-------------|--------|-------------|
| 2026-07-22 | Active | Default report is a focused three-fix Finish Plan; full evidence moves to `/details` | The single dense report buried the core fix and re-check loop | Keep one full explorer; remove advanced evidence | Product Hunt craft depends on immediate value without deleting engine depth | One `ReportViewModel`; one `buildFinishPlan`; deterministic sample; scoped share routes; canon in `knowledge/report-contract.md` |
| 2026-07-22 | Active | Live-product Launch Check vs code-review gate; CodeRabbit is adjacent not partner GTM | AI verification budget spans PR review and post-deploy product readiness | Compete with CodeRabbit; partner as primary GTM | Different jobs; Scout is the peer war | Acquisition = Launch Check; Finish Plan ≤3 invariant; Project watch for retention; docs in market.md / competitors.md |
| 2026-07-22 | Active | Contract edits merge into PI (never wipe learnings); Remember must be visible | PATCH rebuilt PI from contract only; learnings write-only | Keep silent Remember; wipe on edit | Thesis requires durable memory users can see | `mergeContractIntoProductIntelligence`; `ProductMemoryStrip`; claim attaches Project |
| 2026-07-22 | Active | Project product watch on Prisma + recovery scheduler (not orphan Redis) | `scheduled-monitoring.ts` had zero callers | Keep Redis schedules; defer watch | One self-hosted scheduler; durable Project state | `lib/audit/project-watch.ts`; Pro/Studio; regression-only email |
| 2026-07-20 | Active | Product Intelligence vision is company north star; scanner is first observer | Need durable Product judgment across agents/tools, not only URL audits | Stay “QA layer” forever; rewrite product around repo-first | Independence + persistent memory is the moat | Canon in `knowledge/vision.md`; wedge stays URL Flag→Fix→Re-check; see ROADMAP Phase 0–1 |
| 2026-07-20 | Active | Keep three report rubrics; five integrity dimensions are Integrity Engine framework | Vision defines Product/Experience/Design/Implementation/Agent | Migrate UI/scoring to five rubrics now | Thesis (PI + Finish Plan) first; avoid breaking marketing/MCP/scoring | Message/Experience/Reach remain shipped; mapping in `knowledge/integrity-engine.md` |
| 2026-07-20 | Active | Product Intelligence persists on Project; Audit stores Contract snapshot | Per-audit Contract re-inferred every run; no memory | Site-level only; full graph UI now | Projects already exist; snapshot keeps reports immutable | `Project.productIntelligence` JSON; edit updates project + audit; re-check carries PI |
| 2026-07-20 | Active | Finish Plan is the primary Improve artifact (≤3) | Top Priorities existed but unnamed; long flag lists overwhelm | Keep Top Priorities label; dump all flags as the plan | Prioritization is the product | UI/copy → Finish Plan; `buildPlanModePrompt` is agent export; `knowledge/finish-plan.md` |
| 2026-07-20 | Active | Flag detail is Fix-only Markdown box; SeveritySignal is single circle | Why/Evidence/Verify cards duplicated `buildExpertFixPrompt`; Sparkles felt AI-gimmicky; double-ring Critical icon | Keep separate section cards; keep Sparkles; keep outer severity ring | Peak-End + minimalism: invest in the fix prompt surface | `MarkdownPromptBox`; `buildExpertFixPrompt` emits `##` headings; `Wrench`; `severityLabel` = "Critical Flag" |
| 2026-07-20 | Active | Severity is sort-only; meta = SeveritySignal → Rubric → Impact; device-matched evidence | Severity filter pills burned vertical space; Detected pills duplicated severity; dual screenshots confused mobile findings | Keep Critical filter; show healthy green desktop twin; keep truth pills | Hick's Law + mobile density; evidence must match the finding | No severity FilterPills; `buildExpertFixPrompt` Markdown Why/Evidence/Fix/Scope/Verify; `devicesForCheck` never defaults to both |
| 2026-07-23 | Active | Anonymous access is read-time; never persist signup-gate strings as evidence/fix | Live dogfood showed AI flags storing `TRIAGE_LOCKED_*` placeholders; Copy toasted success on gate text; sample UX overpromised vs live | Keep placeholders in DB; UI-only hide; gate all evidence until signup | PRODUCT/report-contract need real evidence + one demonstrated prompt; fake data cannot be un-stripped | Triage persists real evidence/why and null prompts; `promptAccess`/`resolveFixPrompt` gate Copy; plan in `customer-journey-completion-plan.md` |
| 2026-07-23 | Active | Align AGENTS anon wording with PRODUCT: evidence visible + exactly one demonstrated prompt | AGENTS said all prompts gated; PRODUCT/report-contract/QUALITY require one complete demonstrated fix | Change PRODUCT to all-gated; keep divergent docs | One contract prevents agent thrash and empty first-value reports | AGENTS + product skill match PRODUCT.md; marketing sample may unlock; live anon must not |
| 2026-07-20 | Active | 1 anonymous teaser scan then signup; claimed teaser counts toward Free 3 | Need durable acquisition wedge without giving away all fix prompts or unlimited free triage | Gate all scans behind signup; unlimited anon; teaser does not count | Prove value with triage/evidence + one demonstrated fix; earn account for remaining prompts; quota honesty | Anon: exactly 1 new URL (`createAndEnqueueAudit` owns gate); evidence visible; exactly one demonstrated prompt; remaining prompts stripped until claim; claim increments `auditsUsed`; public APIs never return unstripped prompts; Free = 3 lifetime including claimed teaser |
| 2026-07-20 | Active | First-party Help Center + live chat (not Intercom/Zendesk) | Need world-class support portal; chat already in Postgres | Intercom Fin; Plain; docs-only | Own SLA copy, audit context, and copy.ts SSoT; avoid second identity | `/help` catalog in `lib/help/`; escalate via `openSupportChat`; FAQ stays; `/docs/mcp` canonical → `/help/mcp`; no AI Fin until retrieval exists |
| 2026-07-26 | Active | Dedicated audit worker is required | Inline Playwright blocked Next.js rendering, duplicated consumers during hot reload, and silently replayed stalled scans | Optional or default inline worker | Web latency and browser lifecycle need separate process ownership | `FIXFLAGS_PROCESS_ROLE=web\|worker`; `npm run dev` starts both; worker heartbeat is required readiness evidence |
| 2026-07-19 | Active | Full Stripe config required on revenue deploys | Partial Stripe left checkout 503 for paying users | Soft-degrade billing forever | Taking money requires keys + 5 price IDs + webhook secret | `BILLING_REQUIRED` + `validateStripeBillingEnv`; `/api/health.billingConfigured`; see `docs/stripe-setup.md` |
| 2026-07-19 | Active | One new-URL-check quota; credit packs are paid overflow | Copy promised unlimited deterministic at limit while code blocked all new checks | Separate deterministic vs AI pools | One counter matches `auditsUsed` / create-audit gate | Free = 3 lifetime new URL checks; re-checks free; packs $15/$30/$50; monthly only (no annual yet) |
| 2026-07-19 | Active | Zod 4 at app root + pinned better-auth 1.6.22 | Railway `npm ci` failed on better-auth optional zod@^4 peer vs app zod@3; passkey needed hoisted `@better-auth/core` | `legacy-peer-deps`; leave zod@3 | Align peers properly; hoist core for subpath imports | Direct `@better-auth/core`; `overrides` pin auth packages; LLM schemas via `z.toJSONSchema` (`lib/audit/zod-json-schema.ts`); no `.npmrc` peer bypass |
| 2026-07-19 | Active | Preserve JOURNEY flags across triage persist | Journey MVP wrote flags then `clearAuditResults` wiped them before report | Re-create journey flags after triage; run journey after finalize | Triage owns DETERMINISTIC+AI only | `clearAuditResults` filters by source; journey severity soft-penalizes rubric scores |
| 2026-07-19 | Active | Playwright-only audit browser stack | Partial Puppeteer→Playwright migration broke typecheck and capture | Dual stack with type shims; revert to Puppeteer | Single vendor, page-session already Playwright | `lib/audit/screenshot.ts` launches Chromium via Playwright; no Puppeteer on audit path |
| 2026-07-19 | Active | Visual evidence via `performanceData.flagVisualEvidence` | Need GIF/overlay evidence without Flag schema migration | New Flag columns; evidence text URLs only | Matches evidenceAnchors pattern; graceful skip | Finalize calls `tryCaptureVisualEvidenceForAudit`; UI reads `visualUrl` on explorer flags |
| 2026-07-19 | Active | Homepage product Flag output over invented testimonials | Fake quotes destroy trust | Keep empty testimonial carousel | Ship real Flag examples (`reportExamples`, sample explorer); `testimonials.quotes` stays empty | `ReportExamplesSection`; `productEvidence` kept in copy only |
| 2026-07 | Active | Always work on `main` while pre-prod | No customers yet; feature branches added merge overhead without risk reduction | Per-task agent branches and worktrees | Pre-revenue stage prioritizes speed and a single integration line | Agents pull/commit/push on `main`. Coordinate via BOARD.md. Revisit branching when production traffic warrants it. |
| 2026-07 | Active | Passkey as the second factor for 2FA | Users asked for two-factor auth with passkeys; better-auth has no native passkey-as-2FA yet | TOTP-only 2FA; wait for better-auth 2.0 passkey-2FA | Passkeys are phishing-resistant and match the AI-builder audience (device biometrics / security keys) | Enable flow requires a registered passkey first. Backup codes cover recovery. OAuth sign-in is not gated by 2FA (better-auth default). |
| 2026-06 | Superseded | Inline worker as default deployment | Single-service setup was initially simplest for pre-revenue | Separate worker service | Replaced after measured scan-load freezes and duplicate hot-reload consumers | Superseded by the 2026-07-26 dedicated-worker decision |
| 2026-06 | Active | Self-hosted scheduler replaces external cron | Avoid external cron dependency for stuck-audit recovery + nurture | Railway cron, Google Cloud Scheduler | Redis-lock guarded, runs inside worker. No external infra needed. | Defined in `lib/queue/recovery-scheduler.ts`. Cron HTTP endpoints remain for manual trigger. |
| 2026-06 | Active | Knowledge graph is internal-only; public reads through lib/graph/queries.ts | Prevent tight coupling between growth UI and internal graph schema | Direct graph_* table reads in public pages | Enforces MIN_SAMPLE_SIZE gate, decouples schema changes. | Public pages never import graph models directly. Queries layer is the API boundary. |
| 2026-06 | Active | AI two-phase: Triage (cheap, anonymous) → Prescription (post-signup, detailed) | Anonymous users get enough value to sign up; signed-up users get actionable fix prompts | Single-phase AI for all | Triage proves value. Prescription delivers the product. Different pageText limits per phase. | Triage: 2500 chars from fresh HTML. Prescription: 5000 chars from stored metadata. See `lib/audit/page-text-limits.ts`. |
| 2026-07 | Superseded | Feature freeze policy relaxed | Building a solid product requires shipping improvements, not just waiting for users | Focus on core loop quality + distribution | Scan depth, UI polish, security hardening all serve the core loop | |
| 2026-07 | Active | Three rubrics only: Message, Experience, Reach | Containment prevents scope creep; three is memorable | 5+ rubrics, single score | Simple mental model. Maps to how users think about their site. | No plans to add more rubrics. Each must earn its place. |
| 2026-07 | Active | Re-checks are free and unlimited | Core loop habit. User who re-checks sees value. | Gating re-checks by plan | Re-checks build the habit. Gating them destroys the loop. | Not a revenue lever. Never gate. |
| 2026-07 | Active | Marketing copy single source of truth: lib/marketing/copy.ts | Prevents copy drift across components | Copy in components, copy in CMS | Every component reads from one file. Tests enforce compliance. | Banned phrases tested. No hardcoded strings in components. |
| 2026-07 | Active | Brand name: FixFlags (not QualityOS) | Rebrand complete. Name is concrete, action-oriented. | QualityOS | "Flags" = unit of value. "Fix" = outcome. | Keep aligned: Flags not Findings. Rubrics not Areas. Re-check not Re-audit. |
| 2026-07 | Active | Edge middleware: cookie-presence only, no Prisma | Edge runtime cannot import Node modules | Server-side validation at edge | Cookie presence check is UX-only; server validates every protected route. | proxy.ts must stay Prisma-free. Matches both dev and prod cookie name variants. |
| 2026-07 | Active | OAuth provider availability resolved at runtime, not build time | Build-time gating hides buttons in production (creds not present at build) | Build-time env check | OAuth env vars are runtime-only on deployed service. | `useOAuthProviders` hook fetches `/api/auth/providers`. No rebuild needed to add OAuth. |
| 2026-07 | Active | Tech detection for prescription comes from performanceData.detectedTech, not htmlMetadata | htmlMetadata is optimized for triage (short text). Tech detection is richer in performanceData. | Using htmlMetadata | Performance data has structured tech detection, not truncated raw text. | `PrescriptionContext.techStack` → prompt. Separate from htmlMetadata flow. |
| 2026-07 | Active | MIN_SAMPLE_SIZE = 20 distinct sites before programmatic public pages | Prevents publishing data with insufficient statistical significance | Lower threshold (5, 10), no threshold | 20 is a reasonable minimum for benchmark data to be meaningful. | Enforced in `lib/graph/queries.ts`. Not configurable per-page. |
| 2026-07 | Active | eslint.config with a11y + import plugins | Catch accessibility and import issues at lint time | Base ESLint config | Prevention over manual review. | Part of `npm run lint` and CI. |
| 2026-07 | Active | Triage runs for all scans; prescription gated by includeAi + credits | Triage is the anonymous teaser (score, verdict, rubrics). includeAi only enqueues ai-review job. | Skipping triage for anonymous users | Product promise requires AI summary on every scan. | See `docs/audit-pipeline.md`. `triageAt` marks success; degraded path preserves flags. |
| 2026-07 | Active | Non-fatal R2 and AI key boot in production | Crash-looping on missing optional config freezes deploys | Fatal boot on missing R2/AI keys | Service stays up; /api/health reports degraded. Scans fail or degrade clearly. | `storageConfigured`, `aiConfigured` on /api/health. |
| 2026-07 | Active | Poll-time + scheduler dual recovery for stuck audits | Cron alone misses audits stuck between intervals | Cron-only recovery | Every status poll after 15s can requeue; scheduler catches long-stuck. | `recoverAuditJobOnPoll` + `runStuckAuditRecoverySweep`. |
| 2026-07 | Active | Ambient motion restricted to marketing landing surfaces | Rams review: report/dashboard/admin are work surfaces; infinite loops there are visual pollution and wasted cycles | Ambient backdrop everywhere; removing ambient motion entirely | Peach orbs stay a brand signature where visitors are being introduced to the product, never where they work. | `SiteShell backdrop` prop; audit/report shells pass `minimal`. Policy in DESIGN.md Motion + `docs/design-rams-review.md`. |
| 2026-07-28 | Active | Remove gated LLM page-purpose (USE_LLM_PAGE_PURPOSE) and semantic-slop (USE_SEMANTIC_SLOP) features | Both shipped off-by-default as unproven code behind env flags. No product surface wired to them; gated dead code violates the "no fallbacks, designed properly" principle. | Keep gated; promote to default-on; add UI surfaces. | Dead code adds maintenance burden and confusion. Re-add only when a proven product use case justifies them. | `classifyPagePurposeWithLlm`, `filterSlopFlagsWithLlm`, and related env vars removed. `detectPagePurpose` is the sole purpose classifier. |
| 2026-07-28 | Active | Remove inert RUM capture feature | RUM routes, component, and migration were not wired into any product surface. | Wire RUM into the product; keep as internal-only. | Inert features mislead about product capabilities. | Deleted `app/api/rum/*`, `RumMetricsSection.tsx`, and rum migration. |
| 2026-07-28 | Active | Remove dead _ariaSnapshot capture | ARIA snapshots were captured but never consumed downstream. Removed from Playwright capture, runAllChecks params, and ScreenshotResult/ViewportCapture interfaces. | Keep for future a11y validation. | Dead capture adds latency and maintenance burden on every scan. | ariaSnapshot removed from `/screenshot.ts`, `/pipeline/run-page.ts`, and `/checks/index.ts`. |
| 2026-07-28 | Active | ANON_IP_SOFT_LIMIT = 1 | Five anonymous checks before soft-block contradicted the explicit "one teaser scan" product invariant. | Keep at 5; adjust marketing copy. | Product truth: anonymous users get one teaser scan. The limit should enforce that. | `lib/audit/usage.ts` changed from 5 to 1. |
| 2026-07-28 | Active | A11y metadata fallback gated on axeViolations == null (not empty array) | PageSpeed-based fallback had lower precision. An empty axe-core array means axe ran and found no issues — metadata should not second-guess that. Only fall back when axe truly didn't run (null). | Gate on hasAxeResults (falsy for both null and empty); remove fallback entirely. | axe-core is the production a11y source of truth. Metadata fills the gap only when axe is unavailable. | `lib/audit/checks/accessibility.ts:103`: `axeUnavailable = axeViolations == null`. |
| 2026-07-28 | Active | Rename public /api/reports/[id]/monitoring route to /re-check | Per AGENTS.md invariant, "re-check" is the user-facing term; "monitoring" is internal implementation. The HTTP boundary at `/monitoring` leaked the internal term. | Keep as /monitoring; rename all internal usage to recheck. | Public boundaries should use product language. Internal diff-flags.ts and monitoringMode stay untouched. | Route + tests moved; `AuditPageActions.tsx` endpoint updated. |
| 2026-07-28 | Active | Strip full session from getGatedAuditForRequest; return only user.id | The full auth session object (tokens, secrets, provider metadata) was leaked to client consumers via the report route state. | Return full session for SSR; guard client-side access. | Minimal principle: return only what downstream consumers need (user.id). | `fetch-audit.ts` returns `{ user: { id } }` instead of full session. Downstream `load-report-route-state.ts` unaffected. |
| 2026-07-28 | Active | Move severity/rubric/impact label functions to canonical copy/brand.ts | Label functions lived in `lib/utils.ts` while severity meanings lived in `copy/brand.ts`. Split source of truth. | Move meanings to utils; keep labels in utils. | Single source of truth for all copy in `lib/marketing/copy`. | Functions added to `copy/brand.ts`; `utils.ts` re-exports from `@/lib/marketing/copy` for backward compatibility. |
| 2026-07-28 | Active | Add canonical rubricTint to rubric-icons.ts | RubricScoreBar hardcoded its own tint mapping (Message→brand, Experience→success, Reach→info), duplicating the design-token pairing that already existed in rubricIcon. | Define tints alongside score colors in scan-score-color.ts. | Icon+tint is a rubric-level concern; belongs with rubric-icon in a single module. | `rubricTint()` added to `lib/rubric-icons.ts`; RubricScoreBar consumes it. |
