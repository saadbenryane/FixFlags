# Living-review scan progress must be persisted facts

Pending `ScoreRing` motion cannot share a CSS `transform` with the 12-o'clock start angle. A `to`-only `rotate(360deg)` on the same node as `-rotate-90` interpolates -90deg to 360deg and snaps at the bottom. Start-angle belongs on the SVG `transform` attribute; the chase rotates a wrapper `from 0deg to 360deg`.

Page names while capturing already live on `Audit.progressDetail` and `AuditPage`. They never reached the customer because `progressivePayloadFingerprint` ignored `progressDetail` and Agent projection had no page ledger. Reconstruct Agent rows from `AuditPage`, not timers.

A `FAILED` Review is still the same workspace. Unmounting the split shell for a centered error card hides the transcript and makes Retry look like a no-op: polling stopped because SSR was terminal. Resume polling from the effective status, not the original SSR status.

Evidence: local `/report/[id]` scans that sat on a hitching arc, then a generic Check failed with an empty browser console.
