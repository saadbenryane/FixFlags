Drop reference images here (logo mockups, mood boards, screenshots).

Phase 1 will extract from these:
- Logo SVG (mark + wordmark)
- Color palette (light + dark)
- Typography matches
- Radius / shadow rules

Current placeholder assets in `public/brand/` use Fraunces-style wordmark and ochre mark until replaced.
