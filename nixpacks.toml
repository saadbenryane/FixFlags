# Unused. Railway builds via Dockerfile (see railway.toml builder = "DOCKERFILE").
# Do not edit this file for deploy. Kept only so accidental Nixpacks runs do not
# surprise anyone; prefer Dockerfile for Chromium + Playwright packaging.
[phases.setup]
aptPkgs = [
  "fonts-liberation",
  "chromium",
  "ca-certificates",
  "openssl"
]
