# SEO setup (historical / redirect)

Canonical SEO setup is not a standalone doc in this repository.

Use these sources instead:
- `docs/brand-positioning.md` for positioning and core positioning claims.
- `lib/marketing/copy/seo.ts` for route metadata copy.
- `docs/gtm-*.md` and `app/layout.tsx` for metadata and social image policy.
- `README.md` and `AGENTS.md` for deployment checks that validate SEO tags.
