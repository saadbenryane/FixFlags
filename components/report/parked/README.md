# Parked report panes

Preview, Timeline playback, and Canvas UI live beside this folder and are not imported by the default `/report/[id]` shell.

Keep capture harvest (`lib/audit/action-timeline.ts`) and Canvas HTTP (`app/api/reports/[id]/canvases`) until a product unpark.

Default shell: `ReportWorkspaceSplitShell` is Agent beside Report only.
